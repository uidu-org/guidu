import React from 'react';
import { DashletsFooterProps } from './types';

export default function DashletFooter(props: DashletsFooterProps) {
  return (
    <div className="card-footer border-0">
      <p className="text-muted m-0 small">Edit timeframe here</p>
    </div>
  );
}
