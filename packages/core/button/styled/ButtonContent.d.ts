import * as React from 'react';
declare type Props = {
    followsIcon: boolean;
    spacing: string;
    isLoading?: boolean;
};
declare const ButtonContent: React.StatelessComponent<Props>;
export default ButtonContent;
