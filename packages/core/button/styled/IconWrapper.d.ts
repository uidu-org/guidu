import * as React from 'react';
declare type Props = {
    spacing: string;
    isOnlyChild: boolean;
    isLoading?: boolean;
};
declare const IconWrapper: React.StatelessComponent<Props>;
export default IconWrapper;
