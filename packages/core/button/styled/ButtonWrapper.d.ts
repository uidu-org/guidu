import * as React from 'react';
export declare type Props = {
    onClick?: React.MouseEventHandler;
    fit: boolean;
};
declare const ButtonWrapper: React.StatelessComponent<Props>;
export default ButtonWrapper;
