import styled from 'styled-components';

// adding padding to slides accounts for overflow and shadows
const Slide = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export default Slide;
