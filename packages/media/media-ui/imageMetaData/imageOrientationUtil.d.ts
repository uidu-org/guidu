/**
 * Returns true if image rotated 90 or 270 degrees (on it's side)
 */
export declare const isRotated: (orientation: number) => boolean;
export declare const getCssFromImageOrientation: (orientation: number) => string;
