export type ResponsivrProps = {
  sm?: React.ReactNode,
  md?: React.ReactNode,
  lg?: React.ReactNode,
  xl?: React.ReactNode,
  breakpoints?: {
    sm: number;
    md: number;
    lg: number;
    xl: number;
  }
}
