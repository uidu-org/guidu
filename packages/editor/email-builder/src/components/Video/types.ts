export type IconSize = 'small' | 'medium' | 'large';
export type IconColor = 'ruby' | 'white' | 'black';

export type VideoProps = {
  url: string;
  iconType: string;
  iconSize: IconSize;
  iconColor: IconColor;
};
